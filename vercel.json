{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx tsx scripts/migrate-deploy.ts && npx prisma generate && npx tsx scripts/sync-catalog.ts --soft && next build",
  "crons": [
    {
      "path": "/api/cron/release-unpaid-orders",
      "schedule": "0 3 * * *"
    }
  ]
}
